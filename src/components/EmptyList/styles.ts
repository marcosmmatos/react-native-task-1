import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyList: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 60,
  },

  emptyListHeader: {
    color: "#808080",
    fontWeight: "bold",
    marginTop: 16,
    fontSize: 16,
  },

  emptyListContent: {
    color: "#808080",
    fontSize: 16,
  },
});
