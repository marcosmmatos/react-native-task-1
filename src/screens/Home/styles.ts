import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  page: {
    backgroundColor: "#1A1A1A",
    height: "100%",
    flex: 0,
  },

  taskArea: {
    height: 600,
    backgroundColor: "#1A1A1A",
  },

  fixedList: {
    height: 300,
  },
});
